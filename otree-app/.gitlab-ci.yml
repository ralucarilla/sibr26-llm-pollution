variables:
  APP_NAME: ai-fat-2025
  OTREE_AUTH_LEVEL: "STUDY"
  NAMESPACE: ai-fat
  DEPLOYMENT_VARS: "OTREE_REST_KEY, OTREE_PRODUCTION, OTREE_AUTH_LEVEL, OTREE_ADMIN_PASSWORD, DATABASE_URL, BACKEND_URL, MODEL_CONFIG_NAME, API_APP_USER, API_APP_PASSWORD, RECAPTCHA_SITE_KEY, RECAPTCHA_SECRET_KEY"

include:
  - project: "mpib/chm/common/deploy-pipeline"
    ref: "3.0.2" # "2.1.14"
    file: "otree-app.yml"

workflow:
  rules:
  - when: always
